import React from "react";

import ReactDOM from "react-dom/client";

import App from "./App";

// Hier wird die React-App an das HTML-DOM gehängt
const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
