// Objekt erstellen mit den geforderten Eigenschaften
const lieblingsbuch = {
  titel: "Der Herr der Ringe",
  autor: "J.R.R. Tolkien",
  jahr: 1954,
  genres: ["Fantasy", "Abenteuer"],
};

// Gesamtes Objekt ausgeben
console.log(lieblingsbuch);

// Nur den Titel ausgeben
console.log(lieblingsbuch.titel);

// Erstes Genre aus dem Array ausgeben
console.log(lieblingsbuch.genres[0]);
